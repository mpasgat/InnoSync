server:
  port: 8080

spring:
  security:
    unabled: false

  datasource:
    url: jdbc:postgresql://postgres:5432/registration
    username: myuser
    password: mypass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true