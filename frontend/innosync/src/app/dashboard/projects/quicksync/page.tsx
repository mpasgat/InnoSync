import QuickSync from "../../../components/dashboard/quicksync/QuickSyncPage";


export default function QuickSyncPage() {
  // You can use the projectId param to fetch project-specific data in the future
  // const { projectId } = useParams();
  return <QuickSync />;
} 